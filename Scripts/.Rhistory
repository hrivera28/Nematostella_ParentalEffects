load("/Users/hannyrivera/Documents/Stella_Paper/Manuscript/Code/GitRepo/Data/Main_parental.RData")
library(drc)
library(ggplot2)
library(plyr)
library(dplyr)
library(ggpubr)
library(rcompanion)
library(multcomp)
View(all_data)
drm(Alive/(Alive+Dead) ~ Temp,Treatment,Cohort, weights = (Alive+Dead), data = all_data, fct = LL.2(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = all_data, fct = LL.2(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort,Treatment, weights = (Alive+Dead), data = all_data, fct = LL.3u(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort,Treatment, weights = (Alive+Dead), data = all_data, fct = LL2.3(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort,Treatment, weights = (Alive+Dead), data = all_data, fct = LL3(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort,Treatment, weights = (Alive+Dead), data = all_data, fct = LL.3(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort,Treatment, weights = (Alive+Dead), data = all_data, fct = LL.4(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,c("Cohort","Treatment"), weights = (Alive+Dead), data = all_data, fct = LL.2(), type = "binomial")
getMeanFunctions(drc)
getMeanFunctions()
drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Control") fct = LL.2(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Ctrl") fct = LL.2(), type = "binomial")
drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Ctrl"), fct = LL.2(), type = "binomial")
c2<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="STHS"), fct = LL.2(), type = "binomial")
anova(c1,c2)
c1<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Ctrl"), fct = LL.2(), type = "binomial")
anova(c1,c2)
c1<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Ctrl"), fct = LL.2(), type = "binomial", pmodels = list(~factor(Cohort)-1, ~1)
c2<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="STHS"), fct = LL.2(), type = "binomial", pmodels = list(~factor(Cohort)-1, ~1)
c1<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="Ctrl"), fct = LL.2(), type = "binomial", pmodels = list(~factor(Cohort)-1, ~1))
c2<-drm(Alive/(Alive+Dead) ~ Temp,Cohort, weights = (Alive+Dead), data = subset(all_data,Treatment=="STHS"), fct = LL.2(), type = "binomial", pmodels = list(~factor(Cohort)-1, ~1))
c1
c2
anova(c1,c2)
